How to install this project:

You need to install Rails 2.*

After installing it, go to the directory root and type:
rake gems:install
(To install all dependencies)

After that run the server:
./script/server

And open in your browser the URL http://localhost:3000/movie/wsdl to verified if the web service is working correctly
